"""인증 기능 테스트"""\n\nimport pytest\nfrom fastapi.testclient import TestClient\nfrom sqlmodel import Session, SQLModel, create_engine, select\nfrom sqlmodel.pool import StaticPool\n\nfrom app.main import app\nfrom app.database import get_session\nfrom app.models import User, UserRole\nfrom app.auth import auth_manager\n\n\n# 테스트용 데이터베이스 엔진\n@pytest.fixture(name=\"session\")\ndef session_fixture():\n    engine = create_engine(\n        \"sqlite:///:memory:\",\n        connect_args={\"check_same_thread\": False},\n        poolclass=StaticPool,\n    )\n    SQLModel.metadata.create_all(engine)\n    with Session(engine) as session:\n        yield session\n\n\n@pytest.fixture(name=\"client\")\ndef client_fixture(session: Session):\n    def get_session_override():\n        return session\n    \n    app.dependency_overrides[get_session] = get_session_override\n    client = TestClient(app)\n    yield client\n    app.dependency_overrides.clear()\n\n\n@pytest.fixture(name=\"test_user\")\ndef create_test_user(session: Session):\n    """테스트용 사용자 생성"""\n    hashed_password = auth_manager.hash_password(\"TestPassword123!\")\n    user = User(\n        email=\"test@example.com\",\n        hashed_password=hashed_password,\n        name=\"Test User\",\n        role=UserRole.CREATOR,\n        locale=\"ko\",\n        is_active=True,\n        is_email_verified=True,\n        password_login_enabled=True,\n    )\n    session.add(user)\n    session.commit()\n    session.refresh(user)\n    return user\n\n\ndef test_user_registration(client: TestClient, session: Session):\n    """사용자 등록 테스트"""\n    # 등록 전 사용자 확인\n    users_before = session.exec(select(User)).all()\n    initial_count = len(users_before)\n    \n    # 회원가입 요청\n    response = client.post(\"/signup\", data={\n        \"email\": \"newuser@example.com\",\n        \"password\": \"NewPassword123!\",\n        \"name\": \"New User\",\n        \"role\": \"creator\",\n        \"locale\": \"ko\",\n        \"privacy_agreement\": \"on\",\n        \"guidance_agreement\": \"on\",\n    })\n    \n    # 리다이렉트 확인\n    assert response.status_code == 303\n    \n    # 사용자 생성 확인\n    users_after = session.exec(select(User)).all()\n    assert len(users_after) == initial_count + 1\n    \n    # 새 사용자 확인\n    new_user = session.exec(select(User).where(User.email == \"newuser@example.com\")).first()\n    assert new_user is not None\n    assert new_user.name == \"New User\"\n    assert new_user.role == UserRole.CREATOR\n    assert new_user.is_email_verified == True\n\n\ndef test_user_login_success(client: TestClient, test_user):\n    """로그인 성공 테스트"""\n    response = client.post(\"/login\", data={\n        \"email\": \"test@example.com\",\n        \"password\": \"TestPassword123!\",\n        \"locale\": \"ko\",\n    })\n    \n    # 리다이렉트 확인\n    assert response.status_code == 303\n    \n    # 세션 쿠키 확인\n    assert \"session\" in response.cookies\n\n\ndef test_user_login_failure(client: TestClient, test_user):\n    """로그인 실패 테스트"""\n    # 잘못된 비밀번호\n    response = client.post(\"/login\", data={\n        \"email\": \"test@example.com\",\n        \"password\": \"WrongPassword123!\",\n        \"locale\": \"ko\",\n    })\n    \n    assert response.status_code == 400\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_user_login_inactive(client: TestClient, session: Session):\n    """비활성 계정 로그인 테스트"""\n    # 비활성 사용자 생성\n    hashed_password = auth_manager.hash_password(\"TestPassword123!\")\n    inactive_user = User(\n        email=\"inactive@example.com\",\n        hashed_password=hashed_password,\n        name=\"Inactive User\",\n        role=UserRole.CREATOR,\n        locale=\"ko\",\n        is_active=False,\n        is_email_verified=True,\n        password_login_enabled=True,\n    )\n    session.add(inactive_user)\n    session.commit()\n    \n    response = client.post(\"/login\", data={\n        \"email\": \"inactive@example.com\",\n        \"password\": \"TestPassword123!\",\n        \"locale\": \"ko\",\n    })\n    \n    assert response.status_code == 403\n\n\ndef test_user_login_unverified(client: TestClient, session: Session):\n    """미인증 이메일 로그인 테스트"""\n    # 미인증 사용자 생성\n    hashed_password = auth_manager.hash_password(\"TestPassword123!\")\n    unverified_user = User(\n        email=\"unverified@example.com\",\n        hashed_password=hashed_password,\n        name=\"Unverified User\",\n        role=UserRole.CREATOR,\n        locale=\"ko\",\n        is_active=True,\n        is_email_verified=False,\n        password_login_enabled=True,\n    )\n    session.add(unverified_user)\n    session.commit()\n    \n    response = client.post(\"/login\", data={\n        \"email\": \"unverified@example.com\",\n        \"password\": \"TestPassword123!\",\n        \"locale\": \"ko\",\n    })\n    \n    assert response.status_code == 403\n\n\ndef test_user_logout(client: TestClient, test_user):\n    """로그아웃 테스트"""\n    # 먼저 로그인\n    login_response = client.post(\"/login\", data={\n        \"email\": \"test@example.com\",\n        \"password\": \"TestPassword123!\",\n        \"locale\": \"ko\",\n    })\n    \n    # 로그아웃\n    logout_response = client.post(\"/logout\", allow_redirects=False)\n    \n    assert logout_response.status_code == 303\n    assert logout_response.headers[\"location\"] == \"/\"\n    \n    # 세션 쿠키 삭제 확인\n    assert \"session\" in logout_response.cookies\n    assert logout_response.cookies[\"session\"] == \"\"\n\n\ndef test_password_validation():\n    """비밀번호 검증 테스트"""\n    from app.validators import UserRegistration\n    \n    # 유효한 비밀번호\n    valid_data = {\n        \"email\": \"test@example.com\",\n        \"password\": \"ValidPass123!\",\n        \"name\": \"Test User\",\n        \"role\": \"creator\",\n        \"locale\": \"ko\",\n        \"privacy_agreement\": True,\n        \"guidance_agreement\": True,\n    }\n    user = UserRegistration(**valid_data)\n    assert user.password == \"ValidPass123!\"\n    \n    # 너무 짧은 비밀번호\n    with pytest.raises(ValueError) as exc_info:\n        invalid_data = valid_data.copy()\n        invalid_data[\"password\"] = \"Short1!\"\n        UserRegistration(**invalid_data)\n    assert \"at least 8 characters\" in str(exc_info.value)\n    \n    # 대문자 없는 비밀번호\n    with pytest.raises(ValueError) as exc_info:\n        invalid_data = valid_data.copy()\n        invalid_data[\"password\"] = \"lowercase123!\"\n        UserRegistration(**invalid_data)\n    assert \"uppercase letter\" in str(exc_info.value)\n    \n    # 소문자 없는 비밀번호\n    with pytest.raises(ValueError) as exc_info:\n        invalid_data = valid_data.copy()\n        invalid_data[\"password\"] = \"UPPERCASE123!\"\n        UserRegistration(**invalid_data)\n    assert \"lowercase letter\" in str(exc_info.value)\n    \n    # 숫자 없는 비밀번호\n    with pytest.raises(ValueError) as exc_info:\n        invalid_data = valid_data.copy()\n        invalid_data[\"password\"] = \"NoNumbers!\"\n        UserRegistration(**invalid_data)\n    assert \"digit\" in str(exc_info.value)\n    \n    # 특수문자 없는 비밀번호\n    with pytest.raises(ValueError) as exc_info:\n        invalid_data = valid_data.copy()\n        invalid_data[\"password\"] = \"NoSpecial123\"\n        UserRegistration(**invalid_data)\n    assert \"special character\" in str(exc_info.value)\n\n\ndef test_email_validation():\n    """이메일 검증 테스트"""\n    from app.validators import UserRegistration\n    \n    # 유효한 이메일\n    valid_data = {\n        \"email\": \"test@example.com\",\n        \"password\": \"ValidPass123!\",\n        \"name\": \"Test User\",\n        \"role\": \"creator\",\n        \"locale\": \"ko\",\n        \"privacy_agreement\": True,\n        \"guidance_agreement\": True,\n    }\n    user = UserRegistration(**valid_data)\n    assert user.email == \"test@example.com\"\n    \n    # 잘못된 이메일 형식\n    with pytest.raises(ValueError):\n        invalid_data = valid_data.copy()\n        invalid_data[\"email\"] = \"invalid-email\"\n        UserRegistration(**invalid_data)\n\n\nif __name__ == \"__main__\":\n    pytest.main([\"-v\", __file__])"
