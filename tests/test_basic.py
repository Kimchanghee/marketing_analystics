"""기본 기능 테스트"""\n\nimport pytest\nfrom fastapi.testclient import TestClient\nfrom sqlmodel import Session, SQLModel, create_engine\nfrom sqlmodel.pool import StaticPool\n\nfrom app.main import app\nfrom app.database import get_session\n\n\n# 테스트용 데이터베이스 엔진\n@pytest.fixture(name=\"session\")\ndef session_fixture():\n    engine = create_engine(\n        \"sqlite:///:memory:\",\n        connect_args={\"check_same_thread\": False},\n        poolclass=StaticPool,\n    )\n    SQLModel.metadata.create_all(engine)\n    with Session(engine) as session:\n        yield session\n\n\n@pytest.fixture(name=\"client\")\ndef client_fixture(session: Session):\n    def get_session_override():\n        return session\n    \n    app.dependency_overrides[get_session] = get_session_override\n    client = TestClient(app)\n    yield client\n    app.dependency_overrides.clear()\n\n\ndef test_health_check(client: TestClient):\n    """헬스 체크 엔드포인트 테스트"""\n    response = client.get(\"/health\")\n    assert response.status_code == 200\n    data = response.json()\n    assert \"status\" in data\n    assert \"database\" in data\n\n\ndef test_landing_page(client: TestClient):\n    """랜딩 페이지 테스트"""\n    response = client.get(\"/\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_services_page(client: TestClient):\n    """서비스 소개 페이지 테스트"""\n    response = client.get(\"/services\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_personal_plan_page(client: TestClient):\n    """개인 요금제 페이지 테스트"""\n    response = client.get(\"/personal\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_business_plan_page(client: TestClient):\n    """기업 요금제 페이지 테스트"""\n    response = client.get(\"/business\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_support_page(client: TestClient):\n    """고객 지원 페이지 테스트"""\n    response = client.get(\"/support\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_login_page(client: TestClient):\n    """로그인 페이지 테스트"""\n    response = client.get(\"/login\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_signup_page(client: TestClient):\n    """회원가입 페이지 테스트"""\n    response = client.get(\"/signup\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_recovery_page(client: TestClient):\n    """계정 복구 페이지 테스트"""\n    response = client.get(\"/recover\")\n    assert response.status_code == 200\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_sitemap(client: TestClient):\n    """사이트맵 테스트"""\n    response = client.get(\"/sitemap.xml\")\n    assert response.status_code == 200\n    assert \"application/xml\" in response.headers[\"content-type\"]\n\n\ndef test_robots_txt(client: TestClient):\n    """robots.txt 테스트"""\n    response = client.get(\"/robots.txt\")\n    assert response.status_code == 200\n    assert \"text/plain\" in response.headers[\"content-type\"]\n\n\ndef test_404_page(client: TestClient):\n    """404 페이지 테스트"""\n    response = client.get(\"/nonexistent-page\")\n    assert response.status_code == 404\n    assert \"text/html\" in response.headers[\"content-type\"]\n\n\ndef test_pricing_redirect(client: TestClient):\n    """요금제 리다이렉트 테스트"""\n    # 기본 리다이렉트 (personal)\n    response = client.get(\"/pricing\", allow_redirects=False)\n    assert response.status_code == 307\n    assert response.headers[\"location\"] == \"/personal\"\n    \n    # business 타입 리다이렉트\n    response = client.get(\"/pricing?type=business\", allow_redirects=False)\n    assert response.status_code == 307\n    assert response.headers[\"location\"] == \"/business\"\n\n\ndef test_locale_parameter(client: TestClient):\n    """언어 파라미터 테스트"""\n    # 한국어\n    response = client.get(\"/?lang=ko\")\n    assert response.status_code == 200\n    \n    # 영어\n    response = client.get(\"/?lang=en\")\n    assert response.status_code == 200\n    \n    # 일본어\n    response = client.get(\"/?lang=ja\")\n    assert response.status_code == 200\n\n\nif __name__ == \"__main__\":\n    pytest.main([\"-v\", __file__])"
