# 🎨 UI 폴더 - 개선된 구조

## 📁 새로운 폴더 구조

```
ui/
├── components/          # 재사용 가능한 컴포넌트
│   ├── _auth_social_buttons.html   # 소셜 로그인 버튼
│   ├── _alert_messages.html        # 알림 메시지
│   └── _pricing_card.html           # 요금제 카드
│
├── layouts/            # 레이아웃 템플릿
│   ├── base.html              # 기본 레이아웃
│   ├── auth_layout.html       # 인증 페이지 레이아웃
│   └── dashboard_layout.html  # 대시보드 레이아웃
│
├── pages/             # 실제 페이지
│   ├── auth/         # 인증 관련
│   │   ├── login.html
│   │   ├── signup.html
│   │   └── recovery.html
│   │
│   ├── dashboard/    # 대시보드 관련
│   │   ├── creator.html     # 개인 대시보드
│   │   ├── manager.html     # 기업 대시보드
│   │   ├── channels.html    # 채널 관리
│   │   └── profile.html     # 프로필
│   │
│   ├── admin/        # 관리자 도구
│   │   ├── super_admin.html
│   │   ├── creator_detail.html
│   │   └── inquiries.html
│   │
│   └── public/       # 공개 페이지
│       ├── landing.html
│       ├── services.html
│       ├── personal.html
│       ├── business.html
│       └── support.html
│
├── static/            # 정적 파일
│   ├── css/
│   ├── js/
│   ├── img/
│   └── assets/
│
└── templates/         # 레거시 템플릿 (호환성 유지)
    └── ... (기존 파일들)
```

---

## 🎯 장점

### 1. **명확한 분리**
- **Components**: 재사용 가능한 UI 조각
- **Layouts**: 페이지 구조 템플릿
- **Pages**: 실제 페이지 콘텐츠

### 2. **유지보수 용이**
- 관련 파일들이 논리적으로 그룹화됨
- 컴포넌트 수정 시 모든 페이지에 자동 반영

### 3. **코드 재사용**
- 공통 컴포넌트를 여러 페이지에서 include
- DRY (Don't Repeat Yourself) 원칙 준수

### 4. **확장성**
- 새로운 페이지 추가가 쉬움
- 팀 협업 시 충돌 최소화

---

## 🔧 사용 방법

### 컴포넌트 사용하기
```html
<!-- 페이지에서 컴포넌트 포함 -->
{% include 'components/_alert_messages.html' %}
{% include 'components/_auth_social_buttons.html' with auth_mode='login' %}
{% include 'components/_pricing_card.html' with plan='pro' %}
```

### 레이아웃 확장하기
```html
<!-- auth 페이지 -->
{% extends "layouts/auth_layout.html" %}

{% block auth_content %}
<h1>로그인</h1>
<!-- 로그인 폼 -->
{% endblock %}
```

### 새 페이지 추가하기
1. 적절한 카테고리 폴더에 HTML 파일 생성
2. 레이아웃 extends
3. 필요한 컴포넌트 include
4. 라우터에 URL 매핑

---

## 📝 파일 명명 규칙

### Components
- 파일명: `_component_name.html` (언더스코어로 시작)
- 예시: `_pricing_card.html`, `_alert_messages.html`

### Layouts
- 파일명: `layout_name.html`
- 예시: `base.html`, `auth_layout.html`

### Pages
- 파일명: `page_name.html` (설명적인 이름)
- 예시: `login.html`, `creator.html`

---

## 🔄 마이그레이션 가이드

### 라우터 업데이트 (app/routers/)

**이전:**
```python
return templates.TemplateResponse("login.html", context)
```

**변경 후:**
```python
return templates.TemplateResponse("pages/auth/login.html", context)
```

### 템플릿 참조 업데이트

**이전:**
```html
{% extends "base.html" %}
```

**변경 후:**
```html
{% extends "layouts/base.html" %}
```

---

## ✅ 마이그레이션 체크리스트

- [ ] Components 생성 완료
- [ ] Layouts 생성 완료
- [ ] Pages 이동 완료
- [ ] app/main.py 템플릿 경로 업데이트
- [ ] 라우터 파일들 업데이트
  - [ ] app/routers/auth.py
  - [ ] app/routers/dashboard.py
  - [ ] app/routers/admin.py
  - [ ] app/main.py
- [ ] 모든 페이지 렌더링 테스트
- [ ] 레거시 templates/ 폴더 제거 (선택사항)

---

## 🚀 배포 전 확인사항

1. 모든 페이지가 정상적으로 렌더링되는지 확인
2. 컴포넌트 include가 제대로 작동하는지 확인
3. 레이아웃 상속이 정상 작동하는지 확인
4. SEO 메타 태그가 유지되는지 확인

---

**마지막 업데이트**: 2025-11-06  
**구조 개선 버전**: v2.0
