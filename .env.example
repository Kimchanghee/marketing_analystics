# 환경 변수 예시 파일
# 로컬 개발 시 이 파일을 복사하여 .env 파일을 생성하세요
# cp .env.example .env

# 환경 설정
ENVIRONMENT=development

# 데이터베이스 연결 (필수!)
# Cloud Run에서는 반드시 PostgreSQL을 사용해야 합니다
# 로컬 개발 시에만 SQLite 사용 가능
DATABASE_URL=sqlite:///./app/app.db

# Cloud Run용 PostgreSQL 예시:
# DATABASE_URL=postgresql://dbuser:password@host:5432/marketing_analytics

# JWT 토큰용 시크릿 키 (필수!)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production

# 슈퍼 관리자 접근 토큰
SUPER_ADMIN_ACCESS_TOKEN=Ckdgml9788@

# Google Gemini API 키 (선택)
GEMINI_API_KEY=your-gemini-api-key-here
