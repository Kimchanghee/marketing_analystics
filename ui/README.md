# 🎨 UI 폴더

이 폴더는 모든 프론트엔드 관련 파일들을 포함합니다.

## 📁 구조

```
ui/
├── templates/          # Jinja2 HTML 템플릿
│   ├── base.html      # 기본 레이아웃 (헤더, 푸터, 네비게이션)
│   ├── landing.html   # 랜딩 페이지
│   ├── login.html     # 로그인 페이지
│   ├── signup.html    # 회원가입 페이지
│   ├── dashboard.html # 개인 크리에이터 대시보드
│   ├── manager_dashboard.html # 기업 관리자 대시보드
│   ├── super_admin.html # 슈퍼 관리자 페이지
│   └── ...
│
└── static/            # 정적 파일
    ├── css/          # 스타일시트
    │   └── style.css
    ├── js/           # JavaScript 파일
    │   ├── theme.js  # 다크모드 토글
    │   └── slider.js # 슬라이더 기능
    ├── img/          # 이미지 파일
    └── assets/       # 빌드된 자산
```

## 🎯 주요 템플릿

### 공개 페이지
- `landing.html` - 메인 랜딩 페이지
- `services.html` - 서비스 소개
- `personal.html` - 개인 요금제
- `business.html` - 기업 요금제
- `support.html` - 고객 지원

### 인증 페이지
- `login.html` - 로그인
- `signup.html` - 회원가입
- `recovery.html` - 비밀번호 복구

### 대시보드
- `dashboard.html` - 개인 크리에이터 대시보드 (AI PD 통합)
- `manager_dashboard.html` - 기업 관리자 대시보드 (AI PD 통합)
- `channels_manage.html` - 채널 관리
- `profile.html` - 프로필 설정

### 관리자
- `super_admin.html` - 슈퍼 관리자 콘솔
- `manager_inquiries.html` - 문의 관리
- `creator_detail.html` - 크리에이터 상세

## 🎨 스타일 가이드

### CSS 변수 (다크모드 지원)
```css
--primary-color: 라이트/다크에 따라 자동 변경
--background-color: 배경색
--text-color: 텍스트 색상
--border-color: 테두리 색상
```

### 반응형 디자인
- 모바일: < 768px
- 태블릿: 768px - 1024px
- 데스크톱: > 1024px

## 🔧 사용 방법

FastAPI 앱에서 자동으로 로드됩니다:
```python
# app/main.py
UI_DIR = BASE_DIR.parent / "ui"
app.mount("/static", StaticFiles(directory=UI_DIR / "static"))
app.state.templates = Jinja2Templates(directory=str(UI_DIR / "templates"))
```

## 🚀 개발

### 템플릿 수정
1. `ui/templates/` 에서 HTML 파일 수정
2. Jinja2 문법 사용
3. `{{ t['key'] }}` 로 다국어 지원

### 스타일 수정
1. `ui/static/css/style.css` 수정
2. CSS 변수 사용으로 다크모드 자동 지원
3. 반응형 디자인 고려

### JavaScript 추가
1. `ui/static/js/` 에 파일 추가
2. `base.html`에서 로드

## 📝 주의사항

- 모든 경로는 `/static/` 으로 시작
- 다국어 키는 `app/locales/` 에서 관리
- AI PD 기능은 대시보드에 통합되어 있음

